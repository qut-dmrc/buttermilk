tests:
  - name: moderate_${variant_id}_${timestamp}
    display_name: moderate_${variant_id}_${timestamp}
    flow: /workspaces/dev/automod/flows/judge
    data: /tmp/osb_drag_toxic_train.jsonl
    column_mapping:
      source: ${data.source}
      content: ${data.text}
      record_id: ${data.id}
      run_local_models: false
      langchain_model_name: unconfigured