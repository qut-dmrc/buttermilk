
name: buttermilk
job: testing
verbose: true
secret_provider: azure
logger: gcp
models_secret: models
save_dest: gcp
save_dir:
gcp:
  project: dmrc-analysis
  region: us-central1
  bucket: dmrc-analysis


  azure:
    vault: https://suzorvault.vault.azure.net/
    resource_group: rg-suzor_ai

save:
  type: bq
  destination: 'dmrc-platforms.testing.unittest'
  schema: buttermilk/schemas/step.json