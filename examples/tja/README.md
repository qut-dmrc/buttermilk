# TJA Template Analysis Dashboard

This directory contains a Streamlit dashboard for analyzing the results of the TJA A/B testing experiment.

## Running the Dashboard

To run the dashboard, navigate to the root of the `buttermilk` project and run the following command:

```bash
uv run streamlit run examples/tja/streamlit_dashboard.py
```
