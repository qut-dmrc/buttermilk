# @package _global_
job: drag
run:
  dataset:
    uri: gs://dmrc-platforms/data/drag_train.jsonl
    columns:
      record_id: \${data.id}
      content: \${data.alt_text}
  tasks:
    judge:
      experiment_name: ots
      model_type: ots
      models:
      - ShieldGemma
      - LlamaGuard3Local
      - MDJudgeLocalDomain
      - MDJudgeLocalTask
      - LlamaGuard2Local
      - LlamaGuard3LocalInt8