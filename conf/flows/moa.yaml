defaults:
  - _self_
  - moa.steps:
    - agents.manager
    - agents.host
    - agents.judge
  - criteria: trans

ui:  CLIUserAgent #_fake_slack_agent # 

moa:
  orchestrator: Selector
  name: Mixture of Agents directed chat (trans journalists criteria)
  save: ${save}

  steps:
