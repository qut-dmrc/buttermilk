owl:
  id: OWLS
  name: "🦉 Owl"
  description: They will speak up if needed. You don't need to call on them.
  agent_obj: LLMAgent
  num_runs: 1
  variants:
    template: owl
    model: ${llms.general}
  inputs:
    history: history
