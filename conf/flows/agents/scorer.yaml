scorer:
  role: SCORERS
  name: "📊 Scorer"
  agent_obj: LLMScorer
  num_runs: 1
  description: Checks and scores answers against our expected ground truth.
  variants:
    model: ${llms.general}
  parameters:
    template: score
  inputs:
    # criteria: Evaluate how well the provided content aligns with the rules.
    criteria: "*.inputs.parameters.criteria|[0]"
    records: "*.records[]||*.inputs.records[]|[0]"
    expected: "*.records[0].ground_truth||*.inputs.records[0].ground_truth|[0].reasons"
    answers: "[judge,synth][]"
