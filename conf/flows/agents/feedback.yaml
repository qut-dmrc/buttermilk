feedback:
  role: FEEDBACK
  name: "Feedback"
  agent_obj: LLMAgent
  num_runs: 1
  description: Checks for any concerns with the discussion so far.
  variants:
    model: ${llms.general}
  parameters:
    template: feedback
  inputs:
    history: "*[].outputs"
