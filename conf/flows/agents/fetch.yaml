fetch:
  id: FETCH
  role: "🔍 fetch record"
  agent_obj: FetchRecord
  description: A TOOL to retrieve the content of URLs and stored RECORDS required for the discussion.
  data:
    - ${datasets.tja_train}
  inputs:
    prompt: prompt