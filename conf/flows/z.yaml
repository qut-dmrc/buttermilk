# @package flows.zot
defaults:
  - _self_
  - /data: zot
  - /agents:
    - ragzot
  - /agents@observers:
    - host/assistant
  - /agents/interface@ui: proxy  # Use the proxy agent that can dynamically switch UI implementations

orchestrator: buttermilk.orchestrators.groupchat.AutogenOrchestrator
description: Research assistant
parameters:
  human_in_loop: true
