# Simplified test configuration for OSB flow with API server

defaults:
  - /run: api
  - /flows/osb
  - /llms: lite

bm:
  name: test_osb_api
  job: test_flows
  platform: test
  tracing:
    enabled: false  # Disable Weave for faster tests

# Override flows to use only OSB
flows:
  - osb