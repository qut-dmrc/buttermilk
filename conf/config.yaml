defaults:
  - _self_
  - local
  - llms: lite
  - flows: []


ui: console
human_in_loop: true
run: help
bm:
  _target_: buttermilk.bm.BM

  logger_cfg:
  clouds:
  tracing:
    enabled: true
  run_info:
    name: ${name}
    job: ${job}
    platform: local
    max_concurrency: 16
    flow_api: http://flow:8000/flow/
