defaults:
  - _self_
  - local
  - flows: 
    - osb
    - tox
    - trans
  - llms: full
  - run: api

bm:
  name: multi_flow_api
  job: flows

run:
  mode: api
  ui: web
  human_in_loop: true
  flows: ${flows}
  name: ${bm.name}
  job: ${bm.job}