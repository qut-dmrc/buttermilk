defaults:
  - _self_
  - local
  - flows: 
    - osb
  - llms: lite

bm:
  name: osb_console
  job: flows

run:
  mode: console
  ui: console
  human_in_loop: true
  flows: ${flows}
  name: ${bm.name}
  job: ${bm.job}