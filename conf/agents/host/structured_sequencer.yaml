host:
  role: HOST
  name: "🎯 Structured Sequencer"
  agent_obj: buttermilk.agents.flowcontrol.structured_llmhost.StructuredLLMHostAgent
  parameters:
    human_in_loop: ${run.human_in_loop}
    model: ${run.model}
    template: "host_structured_tools"
  inputs:
    task: |
      The purpose of this conversation is to assess a single piece of content. You are the HOST.
      
      Use the available agent tools to coordinate the assessment workflow:
      - Each agent has specific tools you can invoke directly
      - Monitor progress and adapt the workflow based on results
      - Synthesize findings from multiple agents when needed